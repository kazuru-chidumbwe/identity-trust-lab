# Drivers

IdP adapters live here.

| Driver | Status |
| --- | --- |
| `keycloak/` | Normalize path present |

Each driver accepts the same profile JSON contract and emits a normalized trace per `docs/methodology.md`.

| Driver | Status |
| --- | --- |
| `keycloak/` | Live normalize fixture + Case 1 partial |
| `wso2/` | Live normalize fixture + Case 1 partial |
| `common/normalize.py` | Shared field mapper |
