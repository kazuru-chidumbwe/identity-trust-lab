# Profiles

OIDC measurement profiles (JSON). Schema: [`../schemas/profile.schema.json`](../schemas/profile.schema.json).

| ID | Spine | Notes |
| --- | --- | --- |
| `P-TOKEN-SHAPE-v0` | token_endpoint_shape | **Measured** on Case 1 partial (KC vs WSO2) — password grant lab shortcut |
| `P-REFRESH-REUSE-v0` | refresh_token | Future refresh-spine profile — **not** exercised by Case 1 partial |

Do not change pinned profile bytes after a public tag that cites them.
