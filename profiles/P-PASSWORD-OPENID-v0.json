{
  "id": "P-PASSWORD-OPENID-v0",
  "version": "0.1",
  "description": "Lab-only password grant to exercise token-endpoint normalization on both IdPs. Not production-recommended. Authorization Code + PKCE remains the programme baseline for the full Case 1 refresh spine.",
  "flow": "resource_owner_password",
  "spine": "token_endpoint_shape",
  "client": {
    "client_id": "itl-lab-client",
    "pkce": false
  },
  "perturbations": [],
  "assertions": [
    {
      "feature": "password_grant_openid_issues_tokens",
      "expected": "HTTP 200, Bearer, refresh issued, id_token contains sub"
    },
    {
      "feature": "access_token_is_jwt",
      "expected": "JWT-ness of access token compared only after config equivalence for JWT access tokens"
    }
  ]
}
