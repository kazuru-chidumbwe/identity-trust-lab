services:
  wso2is:
    image: wso2/wso2is:7.0.0
    ports:
      - "9443:9443"
    # Lab only. Default admin/admin. Create OIDC app via DCR before live capture.
    # Startup often takes several minutes.
